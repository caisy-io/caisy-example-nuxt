{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "nuxt": "^3.10.3"
  },
  "dependencies": {
    "@caisy/rich-text-vue-renderer": "^0.3.0",
    "graphql": "^16.6.0",
    "graphql-request": "^6.1.0"
  }
}
