{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "nuxt": "^3.1.1"
  },
  "dependencies": {
    "@caisy/rich-text-vue-renderer": "^0.2.1",
    "graphql": "^16.6.0",
    "graphql-request": "^5.1.0"
  }
}
